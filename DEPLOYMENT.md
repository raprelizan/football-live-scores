# دليل النشر على Vercel

## المتطلبات الأساسية

- حساب Vercel (يمكنك إنشاء واحد مجاناً على https://vercel.com)
- حساب GitHub (لربط المستودع)
- Node.js 18+ و pnpm

## خطوات النشر

### 1. تحضير المستودع

```bash
# إنشاء مستودع جديد على GitHub
git init
git add .
git commit -m "Initial commit: Football Live Scores"
git branch -M main
git remote add origin https://github.com/yourusername/football-live-scores.git
git push -u origin main
```

### 2. ربط Vercel بـ GitHub

1. اذهب إلى https://vercel.com/new
2. اختر "Import Git Repository"
3. ابحث عن مستودعك `football-live-scores`
4. اضغط "Import"

### 3. تكوين المتغيرات البيئية

في لوحة تحكم Vercel، أضف المتغيرات البيئية التالية:

```
FOOTBALL_DATA_API_KEY=your_api_key_here
DATABASE_URL=your_database_url
JWT_SECRET=your_jwt_secret
VITE_APP_TITLE=كورة لايف
VITE_APP_LOGO=your_logo_url
```

### 4. إعدادات البناء

- **Build Command**: `pnpm build`
- **Output Directory**: `dist`
- **Install Command**: `pnpm install`

### 5. النشر

بعد تكوين جميع المتغيرات:

1. اضغط "Deploy"
2. انتظر انتهاء البناء (عادة 2-5 دقائق)
3. ستحصل على رابط الموقع الخاص بك

## التحقق من النشر

بعد النشر، تحقق من:

1. **الصفحة الرئيسية**: يجب أن تحمل بدون أخطاء
2. **المباريات**: تحقق من تحميل بيانات المباريات
3. **الصفحات الثانوية**: اختبر جميع الروابط
4. **الوضع الليلي**: تبديل بين الأوضاع
5. **RTL**: تحقق من اتجاه النص العربي
6. **الاستجابة**: اختبر على الهاتف والكمبيوتر

## استكشاف الأخطاء

### خطأ في البناء

```bash
# تحقق من السجلات في Vercel Dashboard
# أو قم بالبناء محلياً للتحقق
pnpm build
```

### مشاكل المتغيرات البيئية

تأكد من:
- جميع المتغيرات المطلوبة موجودة
- لا توجد مسافات إضافية
- القيم صحيحة وليست مشفرة

### مشاكل قاعدة البيانات

```bash
# تحقق من اتصال قاعدة البيانات
# استخدم DATABASE_URL الصحيح
```

## الإعدادات المتقدمة

### اسم المجال المخصص

1. اذهب إلى إعدادات المشروع في Vercel
2. اختر "Domains"
3. أضف مجالك المخصص
4. اتبع التعليمات لتحديث DNS

### متغيرات البيئة حسب المرحلة

يمكنك تعيين متغيرات مختلفة لـ:
- **Production**: الإعدادات الرسمية
- **Preview**: للاختبار قبل الإطلاق
- **Development**: للتطوير المحلي

### إعادة النشر التلقائي

Vercel سيعيد النشر تلقائياً عند:
- دفع تغييرات إلى فرع main
- إنشاء pull request (يُنشئ preview)

## الأداء والتحسينات

### تحسين السرعة

1. **تفعيل الضغط**: Vercel يفعله تلقائياً
2. **التخزين المؤقت**: تم تكوينه في vercel.json
3. **صور محسّنة**: استخدم WebP عند الإمكان
4. **التحميل البطيء**: تم تطبيقه على الصور

### المراقبة

- استخدم Vercel Analytics لمراقبة الأداء
- راقب معدلات الخطأ والاستجابة
- تحقق من استخدام قاعدة البيانات

## الدعم والمساعدة

- **توثيق Vercel**: https://vercel.com/docs
- **مجتمع Vercel**: https://vercel.com/support
- **GitHub Issues**: أنشئ issue في المستودع

## الخطوات التالية

بعد النشر بنجاح:

1. قم بتحديث DNS إذا كنت تستخدم مجالاً مخصصاً
2. أضف Google Analytics لمراقبة الزوار
3. أعد تشغيل اختبارات الأداء
4. راقب السجلات للأخطاء
5. قم بتحديثات منتظمة

## ملاحظات مهمة

- ✅ جميع البيانات الحساسة يجب أن تكون في متغيرات البيئة
- ✅ لا تدفع ملفات `.env` إلى GitHub
- ✅ استخدم HTTPS دائماً
- ✅ قم بنسخ احتياطية منتظمة من قاعدة البيانات
- ✅ راقب استخدام Football-Data.org API (100 طلب/يوم)
